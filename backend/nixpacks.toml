[phases.setup]
nixPkgs = ["python311", "gcc", "libffi"]

[phases.install]
cmds = [
  "pip install fastapi uvicorn pydantic pydantic-settings sqlalchemy asyncpg pymupdf pillow supabase python-multipart httpx alembic"
]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
